<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Permisos necesarios -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:ignore="UnusedAttribute" />
    <uses-permission android:name="android.permission.ACTION_DIAL" />

    <application
        android:name=".Here4UApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Here4u"
        tools:targetApi="31">

        <!-- 🚨 Desactiva la inicialización automática de WorkManager -->
        <meta-data
            android:name="androidx.work.WorkManagerInitializer"
            android:value="disabled" />

        <!-- 🚫 Elimina la inicialización automática de AndroidX Startup -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />

        <!-- ✅ Actividades -->
        <activity
            android:name=".view.journaling.Journaling"
            android:exported="false" />
        <activity
            android:name=".view.emotions.IdentifyingEmotions"
            android:exported="false"
            android:label="@string/title_activity_identifying_emotions"
            android:theme="@style/Theme.Here4u.NoActionBar" />
        <activity
            android:name=".view.login.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.home.home"
            android:exported="false" />
        <activity
            android:name=".view.login.Signup"
            android:exported="false" />
        <activity
            android:name=".view.login.ForgotPassword"
            android:exported="false" />
        <activity
            android:name=".view.exercises.ExercisesActivity"
            android:exported="false" />
        <activity
            android:name=".view.emergency.Emergency"
            android:exported="false" />
        <activity
            android:name=".view.emergency.CreateContact"
            android:exported="false" />
        <activity
            android:name=".view.profile.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".view.emergency.DetailContactActivity"
            android:exported="false" />

        <!-- ✅ File provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


    </application>
</manifest>
